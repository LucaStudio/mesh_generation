# Autogenerated with SMOP 
from smop.core import *
# 

    
@function
def getindices3D(Nx=None,Ny=None,Nz=None,*args,**kwargs):
    varargin = getindices3D.varargin
    nargin = getindices3D.nargin

    ##
#        Project: Fluid - structure interaction on deformable surfaces
#         Author: Luca Di Stasio
#    Institution: ETH Zrich
#                 Institute for Building Materials
# Research group: Computational Physics for Engineering Materials
#        Version: 0.1
#  Creation date: June 27th, 2014
#    Last update: June 27th, 2014
    
    #    Description: 
#          Input: 
#         Output:
    
    ##
    
    # ---> compute indices
    
    # bulk fluid
    
    indicesbulk=zeros(dot(dot((Nx - 2),(Ny - 2)),(Nz - 2)),1)
    for k in arange(1,Nz - 2).reshape(-1):
        for j in arange(1,Ny - 2).reshape(-1):
            for i in arange(1,Nx - 2).reshape(-1):
                indicesbulk[i + dot((j - 1),(Nx - 2)) + dot(dot((k - 1),(Nx - 2)),(Ny - 2)),1]=(i + 1) + dot(j,Nx) + dot(dot(k,Nx),Ny)
    
    indicesinternalbulk=zeros(dot(dot((Nx - 4),(Ny - 4)),(Nz - 4)),1)
    for k in arange(1,Nz - 4).reshape(-1):
        for j in arange(1,Ny - 4).reshape(-1):
            for i in arange(1,Nx - 4).reshape(-1):
                indicesinternalbulk[i + dot((j - 1),(Nx - 4)) + dot(dot((k - 1),(Nx - 4)),(Ny - 4)),1]=(i + 2) + dot((j + 1),Nx) + dot(dot((k + 1),Nx),Ny)
    
    # faces
    
    indicesF1=zeros(dot((Nx - 2),(Ny - 2)),1)
    
    for j in arange(1,Ny - 2).reshape(-1):
        for i in arange(1,Nx - 2).reshape(-1):
            k=0
            indicesF1[i + dot((j - 1),(Nx - 2)),1]=(i + 1) + dot(j,Nx) + dot(dot(k,Nx),Ny)
    
    indicesF2=zeros(dot((Nx - 2),(Nz - 2)),1)
    
    for k in arange(1,Nz - 2).reshape(-1):
        for i in arange(1,Nx - 2).reshape(-1):
            j=0
            indicesF2[i + dot((k - 1),(Nx - 2)),1]=(i + 1) + dot(j,Nx) + dot(dot(k,Nx),Ny)
    
    indicesF3=zeros(dot((Ny - 2),(Nz - 2)),1)
    
    for k in arange(1,Nz - 2).reshape(-1):
        for j in arange(1,Ny - 2).reshape(-1):
            i=copy(Nx)
            indicesF3[j + dot((k - 1),(Ny - 2)),1]=i + dot(j,Nx) + dot(dot(k,Nx),Ny)
    
    indicesF4=zeros(dot((Nx - 2),(Nz - 2)),1)
    
    for k in arange(1,Nz - 2).reshape(-1):
        for i in arange(1,Nx - 2).reshape(-1):
            j=Ny - 1
            indicesF4[i + dot((k - 1),(Nx - 2)),1]=(i + 1) + dot(j,Nx) + dot(dot(k,Nx),Ny)
    
    indicesF5=zeros(dot((Ny - 2),(Nz - 2)),1)
    
    for k in arange(1,Nz - 2).reshape(-1):
        for j in arange(1,Ny - 2).reshape(-1):
            i=1
            indicesF5[j + dot((k - 1),(Ny - 2)),1]=i + dot(j,Nx) + dot(dot(k,Nx),Ny)
    
    indicesF6=zeros(dot((Nx - 2),(Ny - 2)),1)
    
    for j in arange(1,Ny - 2).reshape(-1):
        for i in arange(1,Nx - 2).reshape(-1):
            k=Nz - 1
            indicesF6[i + dot((j - 1),(Nx - 2)),1]=(i + 1) + dot(j,Nx) + dot(dot(k,Nx),Ny)
    
    indicesinternalF1=zeros(dot((Nx - 4),(Ny - 4)),1)
    
    for j in arange(1,Ny - 4).reshape(-1):
        for i in arange(1,Nx - 4).reshape(-1):
            k=2
            indicesinternalF1[i + dot((j - 1),(Nx - 4)),1]=(i + 2) + dot((j + 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalF1=matlabarray(cat([indicesinternalF1],[indicesinternalF1 - dot(Nx,Ny)]))
    indicesinternalF2=zeros(dot((Nx - 4),(Nz - 4)),1)
    
    for k in arange(1,Nz - 4).reshape(-1):
        for i in arange(1,Nx - 4).reshape(-1):
            j=2
            indicesinternalF2[i + dot((k - 1),(Nx - 4)),1]=(i + 2) + dot((j - 1),Nx) + dot(dot((k + 1),Nx),Ny)
    
    indicesinternalF2=matlabarray(cat([indicesinternalF2],[indicesinternalF2 - Nx]))
    indicesinternalF3=zeros(dot((Ny - 4),(Nz - 4)),1)
    
    for k in arange(1,Nz - 4).reshape(-1):
        for j in arange(1,Ny - 4).reshape(-1):
            i=Nx - 1
            indicesinternalF3[j + dot((k - 1),(Ny - 4)),1]=i + dot((j + 1),Nx) + dot(dot((k + 1),Nx),Ny)
    
    indicesinternalF3=matlabarray(cat([indicesinternalF3],[indicesinternalF3 + 1]))
    indicesinternalF4=zeros(dot((Nx - 4),(Nz - 4)),1)
    
    for k in arange(1,Nz - 4).reshape(-1):
        for i in arange(1,Nx - 4).reshape(-1):
            j=Ny - 1
            indicesinternalF4[i + dot((k - 1),(Nx - 4)),1]=(i + 2) + dot((j - 1),Nx) + dot(dot((k + 1),Nx),Ny)
    
    indicesinternalF4=matlabarray(cat([indicesinternalF4],[indicesinternalF4 + Nx]))
    indicesinternalF5=zeros(dot((Ny - 4),(Nz - 4)),1)
    
    for k in arange(1,Nz - 4).reshape(-1):
        for j in arange(1,Ny - 4).reshape(-1):
            i=2
            indicesinternalF5[j + dot((k - 1),(Ny - 4)),1]=i + dot((j + 1),Nx) + dot(dot((k + 1),Nx),Ny)
    
    indicesinternalF5=matlabarray(cat([indicesinternalF5],[indicesinternalF5 - 1]))
    indicesinternalF6=zeros(dot((Nx - 4),(Ny - 4)),1)
    
    for j in arange(1,Ny - 4).reshape(-1):
        for i in arange(1,Nx - 4).reshape(-1):
            k=Nz - 1
            indicesinternalF6[i + dot((j - 1),(Nx - 4)),1]=(i + 2) + dot((j + 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalF6=matlabarray(cat([indicesinternalF6],[indicesinternalF6 + dot(Nx,Ny)]))
    # edges
    
    indicesE1=zeros(Nx - 2,1)
    
    for i in arange(1,Nx - 2).reshape(-1):
        j=1
        k=1
        indicesE1[i,1]=(i + 1) + dot((j - 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesE2=zeros(Ny - 2,1)
    
    for j in arange(1,Ny - 2).reshape(-1):
        i=copy(Nx)
        k=1
        indicesE2[j,1]=i + dot(j,Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesE3=zeros(Nx - 2,1)
    
    for i in arange(1,Nx - 2).reshape(-1):
        j=copy(Ny)
        k=1
        indicesE3[i,1]=(i + 1) + dot((j - 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesE4=zeros(Ny - 2,1)
    
    for j in arange(1,Ny - 2).reshape(-1):
        i=1
        k=1
        indicesE4[j,1]=i + dot(j,Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesE5=zeros(Nz - 2,1)
    
    for k in arange(1,Nz - 2).reshape(-1):
        i=1
        j=1
        indicesE5[k,1]=i + dot((j - 1),Nx) + dot(dot(k,Nx),Ny)
    
    indicesE6=zeros(Nz - 2,1)
    
    for k in arange(1,Nz - 2).reshape(-1):
        i=copy(Nx)
        j=1
        indicesE6[k,1]=i + dot((j - 1),Nx) + dot(dot(k,Nx),Ny)
    
    indicesE7=zeros(Nz - 2,1)
    
    for k in arange(1,Nz - 2).reshape(-1):
        i=copy(Nx)
        j=copy(Ny)
        indicesE7[k,1]=i + dot((j - 1),Nx) + dot(dot(k,Nx),Ny)
    
    indicesE8=zeros(Nz - 2,1)
    
    for k in arange(1,Nz - 2).reshape(-1):
        i=1
        j=copy(Ny)
        indicesE8[k,1]=i + dot((j - 1),Nx) + dot(dot(k,Nx),Ny)
    
    indicesE9=zeros(Nx - 2,1)
    
    for i in arange(1,Nx - 2).reshape(-1):
        j=1
        k=copy(Nz)
        indicesE9[i,1]=(i + 1) + dot((j - 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesE10=zeros(Ny - 2,1)
    
    for j in arange(1,Ny - 2).reshape(-1):
        i=copy(Nx)
        k=copy(Nz)
        indicesE10[j,1]=i + dot(j,Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesE11=zeros(Nx - 2,1)
    
    for i in arange(1,Nx - 2).reshape(-1):
        j=copy(Ny)
        k=copy(Nz)
        indicesE11[i,1]=(i + 1) + dot((j - 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesE12=zeros(Ny - 2,1)
    
    for j in arange(1,Ny - 2).reshape(-1):
        i=1
        k=copy(Nz)
        indicesE12[j,1]=i + dot(j,Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalE1=zeros(Nx - 4,1)
    
    for i in arange(1,Nx - 4).reshape(-1):
        j=2
        k=2
        indicesinternalE1[i,1]=(i + 2) + dot((j - 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalE1=matlabarray(cat([indicesinternalE1],[indicesinternalE1 - dot(Nx,Ny)],[indicesinternalE1 - Nx - dot(Nx,Ny)],[indicesinternalE1 - Nx]))
    indicesinternalE2=zeros(Ny - 4,1)
    
    for j in arange(1,Ny - 4).reshape(-1):
        i=Nx - 1
        k=2
        indicesinternalE2[j,1]=i + dot((j + 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalE2=matlabarray(cat([indicesinternalE2],[indicesinternalE2 - dot(Nx,Ny)],[indicesinternalE2 + 1 - dot(Nx,Ny)],[indicesinternalE2 + 1]))
    indicesinternalE3=zeros(Nx - 4,1)
    
    for i in arange(1,Nx - 4).reshape(-1):
        j=Ny - 1
        k=2
        indicesinternalE3[i,1]=(i + 2) + dot((j - 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalE3=matlabarray(cat([indicesinternalE3],[indicesinternalE3 - dot(Nx,Ny)],[indicesinternalE3 - dot(Nx,Ny) + Nx],[indicesinternalE3 + Nx]))
    indicesinternalE4=zeros(Ny - 4,1)
    
    for j in arange(1,Ny - 4).reshape(-1):
        i=2
        k=2
        indicesinternalE4[j,1]=i + dot((j + 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalE4=matlabarray(cat([indicesinternalE4],[indicesinternalE4 - dot(Nx,Ny)],[indicesinternalE4 - dot(Nx,Ny) - 1],[indicesinternalE4 - 1]))
    indicesinternalE5=zeros(Nz - 4,1)
    
    for k in arange(1,Nz - 4).reshape(-1):
        i=2
        j=2
        indicesinternalE5[k,1]=i + dot((j - 1),Nx) + dot(dot((k + 1),Nx),Ny)
    
    indicesinternalE5=matlabarray(cat([indicesinternalE5],[indicesinternalE5 - 1],[indicesinternalE5 - Nx],[indicesinternalE5 - 1 - Nx]))
    indicesinternalE6=zeros(Nz - 4,1)
    
    for k in arange(1,Nz - 4).reshape(-1):
        i=Nx - 1
        j=2
        indicesinternalE6[k,1]=i + dot((j - 1),Nx) + dot(dot((k + 1),Nx),Ny)
    
    indicesinternalE6=matlabarray(cat([indicesinternalE6],[indicesinternalE6 + 1],[indicesinternalE6 - Nx],[indicesinternalE6 + 1 - Nx]))
    indicesinternalE7=zeros(Nz - 4,1)
    
    for k in arange(1,Nz - 4).reshape(-1):
        i=Nx - 1
        j=Ny - 1
        indicesinternalE7[k,1]=i + dot((j - 1),Nx) + dot(dot((k + 1),Nx),Ny)
    
    indicesinternalE7=matlabarray(cat([indicesinternalE7],[indicesinternalE7 + 1],[indicesinternalE7 + Nx],[indicesinternalE7 + 1 + Nx]))
    indicesinternalE8=zeros(Nz - 4,1)
    
    for k in arange(1,Nz - 4).reshape(-1):
        i=2
        j=Ny - 1
        indicesinternalE8[k,1]=i + dot((j - 1),Nx) + dot(dot((k + 1),Nx),Ny)
    
    indicesinternalE8=matlabarray(cat([indicesinternalE8],[indicesinternalE8 - 1],[indicesinternalE8 + Nx],[indicesinternalE8 - 1 + Nx]))
    indicesinternalE9=zeros(Nx - 4,1)
    
    for i in arange(1,Nx - 4).reshape(-1):
        j=2
        k=Nz - 1
        indicesinternalE9[i,1]=(i + 2) + dot((j - 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalE9=matlabarray(cat([indicesinternalE9],[indicesinternalE9 + dot(Nx,Ny)],[indicesinternalE9 - Nx],[indicesinternalE9 - Nx + dot(Nx,Ny)]))
    indicesinternalE10=zeros(Ny - 4,1)
    
    for j in arange(1,Ny - 4).reshape(-1):
        i=Nx - 1
        k=Nz - 1
        indicesinternalE10[j,1]=i + dot((j + 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalE10=matlabarray(cat([indicesinternalE10],[indicesinternalE10 + dot(Nx,Ny)],[indicesinternalE10 + 1],[indicesinternalE10 + 1 + dot(Nx,Ny)]))
    indicesinternalE11=zeros(Nx - 4,1)
    
    for i in arange(1,Nx - 4).reshape(-1):
        j=Ny - 1
        k=Nz - 1
        indicesinternalE11[i,1]=(i + 2) + dot((j - 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalE11=matlabarray(cat([indicesinternalE11],[indicesinternalE11 + dot(Nx,Ny)],[indicesinternalE11 + Nx],[indicesinternalE11 + Nx + dot(Nx,Ny)]))
    indicesinternalE12=zeros(Ny - 4,1)
    
    for j in arange(1,Ny - 4).reshape(-1):
        i=2
        k=Nz - 1
        indicesinternalE12[j,1]=i + dot((j + 1),Nx) + dot(dot((k - 1),Nx),Ny)
    
    indicesinternalE12=matlabarray(cat([indicesinternalE12],[indicesinternalE12 + dot(Nx,Ny)],[indicesinternalE12 - 1],[indicesinternalE12 - 1 + dot(Nx,Ny)]))
    # corners
    
    indicesC1=1
    indicesC2=copy(Nx)
    indicesC3=Nx + dot(Nx,(Ny - 1))
    indicesC4=1 + dot(Nx,(Ny - 1))
    indicesC5=1 + dot(dot(Nx,Ny),(Nz - 1))
    indicesC6=Nx + dot(dot(Nx,Ny),(Nz - 1))
    indicesC7=Nx + dot(Nx,(Ny - 1)) + dot(dot(Nx,Ny),(Nz - 1))
    indicesC8=1 + dot(Nx,(Ny - 1)) + dot(dot(Nx,Ny),(Nz - 1))
    #A = -1-Nx-Nx*Ny;
    B=- Nx - dot(Nx,Ny)
    #C = +1-Nx-Nx*Ny;
    D=- 1 - dot(Nx,Ny)
    E=dot(- Nx,Ny)
    F=+ 1 - dot(Nx,Ny)
    #G = -1+Nx-Nx*Ny;
    H=+ Nx - dot(Nx,Ny)
    #I = +1+Nx-Nx*Ny;
    L=- 1 - Nx
    M=- Nx
    N=+ 1 - Nx
    O=- 1
    P=+ 1
    Q=- 1 + Nx
    R=+ Nx
    S=+ 1 + Nx
    #T = -1-Nx+Nx*Ny;
    U=- Nx + dot(Nx,Ny)
    #V = +1-Nx+Nx*Ny;
    Z=- 1 + dot(Nx,Ny)
    K=dot(+ Nx,Ny)
    J=+ 1 + dot(Nx,Ny)
    #Y = -1+Nx+Nx*Ny;
    X=+ Nx + dot(Nx,Ny)
    #W = +1+Nx+Nx*Ny;
    
    l=2 + Nx + dot(Nx,Ny)
    indicesinternalC1=matlabarray(cat([l],[l + B],[l + D],[l + E],[l + L],[l + M],[l + O]))
    l=(Nx - 1) + Nx + dot(Nx,Ny)
    indicesinternalC2=matlabarray(cat([l],[l + B],[l + E],[l + F],[l + M],[l + N],[l + P]))
    l=dot(Nx,Ny) - Nx - 1 + dot(Nx,Ny)
    indicesinternalC3=matlabarray(cat([l],[l + E],[l + F],[l + H],[l + P],[l + R],[l + S]))
    l=2 + dot(Nx,(Ny - 2)) + dot(Nx,Ny)
    indicesinternalC4=matlabarray(cat([l],[l + D],[l + E],[l + H],[l + O],[l + Q],[l + R]))
    l=2 + Nx + dot(Nx,Ny) + dot(dot(Nx,Ny),(Nz - 2))
    indicesinternalC5=matlabarray(cat([l],[l + L],[l + M],[l + O],[l + U],[l + K],[l + Z]))
    l=(Nx - 1) + Nx + dot(dot(Nx,Ny),(Nz - 2))
    indicesinternalC6=matlabarray(cat([l],[l + M],[l + N],[l + P],[l + U],[l + K],[l + J]))
    l=dot(Nx,Ny) - Nx - 1 + dot(dot(Nx,Ny),(Nz - 2))
    indicesinternalC7=matlabarray(cat([l],[l + P],[l + R],[l + S],[l + K],[l + J],[l + X]))
    l=2 + dot(Nx,(Ny - 2)) + dot(dot(Nx,Ny),(Nz - 2))
    indicesinternalC8=matlabarray(cat([l],[l + O],[l + Q],[l + R],[l + Z],[l + K],[l + X]))
    return indicesbulk,indicesinternalbulk,indicesF1,indicesF2,indicesF3,indicesF4,indicesF5,indicesF6,indicesinternalF1,indicesinternalF2,indicesinternalF3,indicesinternalF4,indicesinternalF5,indicesinternalF6,indicesE1,indicesE2,indicesE3,indicesE4,indicesE5,indicesE6,indicesE7,indicesE8,indicesE9,indicesE10,indicesE11,indicesE12,indicesinternalE1,indicesinternalE2,indicesinternalE3,indicesinternalE4,indicesinternalE5,indicesinternalE6,indicesinternalE7,indicesinternalE8,indicesinternalE9,indicesinternalE10,indicesinternalE11,indicesinternalE12,indicesC1,indicesC2,indicesC3,indicesC4,indicesC5,indicesC6,indicesC7,indicesC8,indicesinternalC1,indicesinternalC2,indicesinternalC3,indicesinternalC4,indicesinternalC5,indicesinternalC6,indicesinternalC7,indicesinternalC8